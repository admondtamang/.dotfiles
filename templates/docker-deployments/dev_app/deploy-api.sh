#!/usr/bin/env bash

cd /home/ubuntu/dev_app/dev_api

# docker compose up --build --detach --remove-orphans  --scale dev_api=1

echo "hello"

#docker-compose exec api python manage.py migrate
# docker system prune -f
