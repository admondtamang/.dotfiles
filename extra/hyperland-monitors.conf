-- See https://wiki.hypr.land/Configuring/Basics/Monitors/
-- List current monitors and supported resolutions with: hyprctl monitors all

local omarchy_gdk_scale = 1
local omarchy_monitor_scale = 1

hl.env("GDK_SCALE", tostring(omarchy_gdk_scale))

-- Laptop panel (AU Optronics 0xD7A7): native res, highest refresh
hl.monitor({ output = "eDP-2", mode = "1920x1200@165", position = "1920x0", scale = 1 })

-- External monitor (Dell S2421HN): highest supported refresh, placed to the left
hl.monitor({ output = "HDMI-A-1", mode = "1920x1080@74.97", position = "0x0", scale = 1 })

-- Fallback for any other monitor plugged in later
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = omarchy_monitor_scale })

-- Workspaces 1-3 on the external monitor, 4-10 on the laptop panel
hl.workspace_rule({ workspace = "1", monitor = "HDMI-A-1", default = true })
hl.workspace_rule({ workspace = "2", monitor = "HDMI-A-1" })
hl.workspace_rule({ workspace = "3", monitor = "HDMI-A-1" })
for i = 4, 10 do
  hl.workspace_rule({ workspace = tostring(i), monitor = "eDP-2", default = (i == 4) })
end

-- Portrait/rotated secondary monitor (transform: 1 = 90°, 3 = 270°).
-- hl.monitor({ output = "DP-2", mode = "preferred", position = "auto", scale = 1, transform = 1 })
